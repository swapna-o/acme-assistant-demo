# --- Core (required) ---
numpy>=1.24

# --- Answering with Claude (optional; falls back to extractive if absent) ---
anthropic>=0.40

# --- Optional: load .env files ---
python-dotenv>=1.0

# --- Optional embedders ---
# voyageai>=0.2                 # RAG_EMBEDDER=voyage  (needs VOYAGE_API_KEY)
# sentence-transformers>=2.2    # RAG_EMBEDDER=sentence-transformers

# --- Optional connectors ---
# google-api-python-client>=2.0 # Google Drive / Docs
# google-auth>=2.0
# msal>=1.24                    # SharePoint / Microsoft Graph
# requests>=2.28

# --- Testing ---
pytest>=7.0
